package:
  name: image_processing_project
  version: "0.0.0"

source:
  path: ../..

build:
  number: 0

requirements:
  host:
    - python
    - setuptools

  run:
    - python

test:
  requires:
    - pytest
  imports:
    - image_processing_project

about:
  home: add_url_here
  license: MIT License
